# amiTea

Start the local server from this directory:

```bash
python3 dev_server.py
```

Open http://localhost:8000. The development server disables browser caching so
changes to HTML, CSS, and JavaScript appear after a normal refresh.